# Server Environment Variables

# Server Port
PORT=3000

# Database URL (PostgreSQL)
# For local development:
# DATABASE_URL=postgresql://localhost:5432/habiti
# For production (Render/Railway/Neon):
DATABASE_URL=postgresql://user:password@host:5432/database

# JWT Secret (generate a secure random string)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# Allowed Origins for CORS (comma-separated for multiple)
# For production, set this to your frontend URL
CORS_ORIGIN=https://your-frontend-url.vercel.app
